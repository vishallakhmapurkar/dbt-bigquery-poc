{
  "spec": {
    "source_name": "dbt_vlakhmapurkar",
    "database": "dbt-hackathon-genai",
    "schema": "dbt_vlakhmapurkar",
    "tables": [
      {
        "name": "customers",
        "primary_key": "customer_id",
        "description": "One record per customer.",
        "columns": [
          { "name": "customer_id", "type": "integer", "description": "Primary key" },
          { "name": "first_name", "type": "string", "description": "Customer first name" },
          { "name": "last_name", "type": "string", "description": "Customer last name" }
        ]
      },
      {
        "name": "orders",
        "primary_key": "order_id",
        "description": "One record per order. Includes cancelled and deleted orders.",
        "columns": [
          { "name": "order_id", "type": "integer", "description": "Primary key" },
          { "name": "customer_id", "type": "integer", "description": "Foreign key to customers" },
          { "name": "order_date", "type": "date", "description": "Order creation timestamp" },
          { "name": "status", "type": "string", "description": "Order status" }
        ]
      }
    ],
    "generate_marts": true,
    "generate_model_schema_yml": true
  },
  "options": {
    "staging_materialization": "view",
    "mart_materialization": "table",
    "naming_convention_staging_prefix": "stg_",
    "naming_convention_mart_suffix": "_mart",
    "include_docs": true,
    "include_tests": false
  }
}
